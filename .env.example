# Application
APP_MODE=dev

# PostgreSQL
DATABASE_URL=postgres://postgres:postgres@localhost:5432/payslip-generation-system?sslmode=disable

# Required for JWT signing and verification
JWT_SECRET=replace-with-a-long-random-secret
